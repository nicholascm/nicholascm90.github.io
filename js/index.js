/*$(document).ready(function() {

  $(window).scroll(function() {
    var scroll = $(window).scrollTop();
    var offsetTop = $('.inactiveIcon').offset().top;
    var offsetTop2 = $('.inactiveIconLevel').offset().top; 
        if($(window).scrollTop() > offsetTop-420) 
          {       
            $(".inactiveIcon").addClass("animated zoomOutUp");
          }
          
    /*if (scroll >= 250) {
      $(".inactiveIcon").addClass("opaque");
    } else if (scroll > 300) {
      $(".inactiveIconLevel2").addClass("opacity"); 
    }else {
    }
  }); 
});

*/